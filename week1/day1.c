// Structure of a C program: #include, main(), printf, return 0.
#include <stdio.h> 

int main(){
   int a = 20;
   int b;
   //data-type: it defines what type of value will be store 
   //variable: its stores value it can't be a keyword  
   //format 
   //in c we have 32 keyword. ex: if , int float char etc 
printf("enter any number\n");
scanf("%d",&b);
printf("value of b :%d", b);
return 0;
}